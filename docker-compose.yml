services:
  dev:
    build:
      context: .
      target: dev
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    working_dir: /app
    stdin_open: true
    tty: true

volumes:
  node_modules:
