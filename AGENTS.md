# Agents

Thin routing guide — read [add_header_CONTEXT.md](add_header_CONTEXT.md) first, then [docs/engineering/agent_bootstrap.md](docs/engineering/agent_bootstrap.md) when implementing.

Durable docs live under `docs/`. Folder rules under `.cursor/rules/` apply when editing matching paths.

**Dev loop is Docker-only** — use `make setup`, `make check`, `make build`. See [docs/engineering/docker.md](docs/engineering/docker.md).
